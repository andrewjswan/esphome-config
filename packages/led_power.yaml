substitutions:
  energy_method: left

logger:
  logs:
    sensor: WARN

web_server:
  version: 3
  sorting_groups:
    - id: sorting_group_power
      name: "Power"
      sorting_weight: 5

number:
  # Power Supply
  - !include ../common/number/power_supply.yaml

sensor:
  # Current
  - !include ../common/sensor/led_current.yaml

  # Power
  - !include ../common/sensor/led_power.yaml

  # Energy Today Usage
  - !include ../common/sensor/energy_today_usage.yaml
