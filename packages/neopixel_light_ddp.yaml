external_components:
  - source:
      type: git
      url: https://github.com/KaufHA/common
    components: [ddp]

ddp:

light:
  # Hyperion / WLED / DDP Effects
  - id: !extend neopixel_led
    effects:
      - addressable_ddp:
          name: WLED / DDP
          timeout: 10s
          disable_gamma: true
